server:
  port: 8003
spring:
  application:
    name: eurekaServer
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://127.0.0.1:8004/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 0
    

